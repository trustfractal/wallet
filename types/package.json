{
  "name": "@fractalwallet/types",
  "version": "0.1.0",
  "private": true,
  "main": "./lib/index.js",
  "typings": "./lib/index.d.ts",
  "files": [
    "lib/**/*"
  ],
  "scripts": {
    "clean": "rm -rf ./lib",
    "build": "yarn clean && yarn build:ts",
    "build:ts": "tsc --declaration -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^4.2.3"
  }
}
