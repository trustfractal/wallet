enum RoutesPaths {
  CONNECT_WALLET = "/connect-wallet",
  CREDENTIALS_SHOW = "/credentials/:id",
  CREDENTIALS_INDEX = "/credentials",
  REQUESTS_SHOW = "/requests/:id",
  REQUESTS_INDEX = "/requests",
  HOME = "/",
}

export default RoutesPaths;
